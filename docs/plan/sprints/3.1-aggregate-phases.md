# Sprint 3.1 — Aggregate phases 5+

**Epic:** E3 Solo evaluation · **Gate:** none · **Status:** `not_started`

## Goal

Turns 5 onward in aggregate — development (5–7) and execution (8+) — so a full solo run produces a
phase-end state rather than stopping at the opening gate.

## Why this next

E2 is closed and **its gate was deferred for a reason that names this sprint.** The opening produces
pass/fail and nothing else — which is right for fitness, because §3 will not let opening strength
breed out every control deck, and is a poor instrument for measuring a policy gap. G3's signal is
real (17.72σ on the finer counters, and a perfect qualitative separation on the land rule) and the
binary gate at a low bar could only see it at 1.14×.

A continuous objective is what G3 needs re-measuring against, and it does not exist until phases 5+
do.

## Exit criteria

- [ ] Development and execution phases in aggregate, with what "aggregate" means written down
- [ ] A full solo run produces a phase-end state
- [ ] A continuous score, and a statement of what it is a score *of*
- [ ] **G3 re-measured** against it, recorded beside the 2.3 numbers rather than over them
- [ ] **G1 re-measured** now that more is simulated, recorded beside 0.9325
- [ ] `make check` green at ≥95% line and branch

## Tasks

- [ ] **T1 — The handoff.** What carries from turn 4 into turn 5.
      *Done when:* the 13-byte opening vector is sufficient input, or it is extended and the reason
      is recorded — not quietly widened.
- [ ] **T2 — Development, turns 5–7, in aggregate.**
      *Done when:* §3's argument is honoured — state is dominated by totals rather than order from
      turn 5 on — and the aggregation is stated rather than implied.
- [ ] **T3 — Execution, turns 8+, in aggregate.**
- [ ] **T4 — A continuous score.**
      *Done when:* it is integers or fixed-point accumulated in index order (§17.1), and what it
      claims to measure is written down before it is used for anything.
- [ ] **T5 — Re-measure G3.**
      *Done when:* both criteria are applied, the numbers sit beside 2.3's, and the verdict is
      passed, failed, or deferred again with a reason.
- [ ] **T6 — Re-measure G1.**
      *Done when:* the new coverage is recorded beside 0.9325 with the inert share quoted next to
      it, exactly as 1.2.1 required.
- [ ] **T7 — Retro and amendments.**

## Decisions to make

| Decision | Status |
| -------- | ------ |
| What the continuous score measures | **Open.** §3 says the opening is a gate and phases 5+ carry the fitness, but not what the scalar is. "Reached turn 5 functional" was the opening's question; this needs its own, decided before the score is built or it becomes whatever was convenient to accumulate |
| How aggregate is aggregate | **Open.** §3's argument is that order stops mattering from turn 5 — that is a claim, and the aggregation should be the cheapest thing that honours it rather than a turn loop with the turns hidden |
| Whether the opening vector is enough to hand over | **Open.** It was built to answer the opening's question, not to be an initial condition |

## Risks carried in

- **A gate threshold in standard errors leaves the sample size blank** (2.3). Significance is free at
  scale. Anything graded here needs an effect size beside it, and so does G4 in 3.3.
- **The skill gap is ~88% mulligan, and that component is the same on every deck** (2.3). A scalar
  gap is the wrong summary; report the components when G3 is re-measured.
- **G1 counted a clause as modelled when the opcode exists, but the metacard cannot carry the amount
  the opcode needs** (2.2) — draw and cost reduction both assume one. Widening the phases re-opens
  G1 and this is part of what it will find.
- **Inert is relative to what is simulated** (1.2.1). 90.3% of clauses are inert for turns 1–4; that
  falls here, and G1 falls with it. That is the model growing rather than breaking, and the inert
  share is quoted beside the fraction, always.
- **A defect can hide from a state vector** (2.2). The opening's reports the pool *after* everything
  untaps, so within-turn timing is invisible through it. Whatever this sprint's vector omits will
  hide the same way.
